# Local Sample Index

Sample indexes are generated on each user's machine and are intentionally not
committed. They can contain absolute paths to Ableton Packs, User Library files,
and project-collected samples.

Use `ableton_bridge.sample_index` or `ableton_bridge.sample_picker` to create and
query a local index after installation.
